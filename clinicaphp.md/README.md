# Dental Clinics CMS - PHP версия

Полнофункциональная CMS для каталога стоматологических клиник на PHP + MySQL + JavaScript.

## 📋 Требования

- PHP 8.1+
- MySQL 8.0+
- Apache/Nginx с mod_rewrite
- PDO расширение PHP

## 🚀 Установка

### 1. Загрузка файлов

Загрузите все файлы из папки `clinicaphp.md` на ваш хостинг или локальный сервер.

### 2. Настройка базы данных

1. Создайте базу данных MySQL:
```sql
CREATE DATABASE dental_clinics CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. Импортируйте структуру таблиц:
```bash
mysql -u root -p dental_clinics < database/structure.sql
```

### 3. Настройка конфигурации

Откройте файл `config/database.php` и укажите данные для подключения к БД:

```php
return [
    'host' => 'localhost',
    'dbname' => 'dental_clinics',
    'username' => 'ваш_логин',
    'password' => 'ваш_пароль',
    // ...
];
```

Откройте файл `config/config.php` и настройте:

```php
define('BASE_URL', 'http://ваш-домен.ru/clinicaphp.md');
define('ADMIN_PASSWORD', 'надежный_пароль');
```

### 4. Права доступа

Убедитесь, что папка `images/` имеет права на запись:
```bash
chmod 755 images/
```

### 5. Проверка .htaccess

Убедитесь, что Apache поддерживает mod_rewrite и .htaccess работает.

## 📁 Структура проекта

```
clinicaphp.md/
├── administrator/          # Административная панель
├── assets/                 # CSS, JS, изображения
├── components/             # Компоненты (контроллеры и шаблоны)
├── config/                 # Конфигурация
├── database/              # SQL файлы
├── images/                # Загруженные изображения
├── includes/              # Основные классы
├── language/              # Языковые файлы
├── api/                   # API эндпоинты
├── index.php              # Главная точка входа
└── .htaccess             # Настройки Apache
```

## 🔐 Доступ к админке

URL: `http://ваш-домен.ru/clinicaphp.md/administrator/`

Логин: `admin` (настраивается в `config/config.php`)
Пароль: указывается в `config/config.php` (по умолчанию `admin123`)

**⚠️ ВАЖНО:** Смените пароль администратора после установки!

## 🌐 Поддерживаемые языки

- Русский (ru)
- Румынский (ro)

Переключение языка происходит автоматически через URL или параметр `?lang=ru` / `?lang=ro`

## 📚 Основные функции

- ✅ Каталог клиник с фильтрацией
- ✅ Детальные страницы клиник
- ✅ Система рейтингов (D-Score)
- ✅ Административная панель
- ✅ Управление городами и районами
- ✅ Управление услугами клиник
- ✅ Система записи на прием
- ✅ Мультиязычность (RU/RO)
- ✅ SEO оптимизация
- ✅ Адаптивный дизайн

## 🔧 API эндпоинты

Все API запросы выполняются через `/api/`:

- `GET /api/clinics` - Список клиник
- `GET /api/clinics/{slug}` - Данные клиники
- `GET /api/cities` - Список городов
- `POST /api/bookings` - Создание записи

## 🎨 Настройка дизайна

Стили находятся в `assets/css/style.css`
Скрипты в `assets/js/main.js`

## 📝 Миграции и обновления

При обновлении структуры БД используйте SQL файлы в папке `database/`

## 🐛 Отладка

Включите режим отладки в `config/config.php`:
```php
define('DEBUG_MODE', true);
```

## 📞 Поддержка

При возникновении проблем проверьте:
1. Права доступа к файлам и папкам
2. Настройки подключения к БД
3. Логи ошибок PHP
4. Работу mod_rewrite (для ЧПУ)

## ⚡ Производительность

Для повышения производительности рекомендуется:
- Включить кеширование на сервере
- Использовать CDN для статических файлов
- Настроить индексы в БД
- Использовать PHP OPcache

## 🔒 Безопасность

- Регулярно обновляйте пароли
- Используйте HTTPS
- Ограничьте доступ к папке `config/`
- Регулярно делайте резервные копии БД


