# Инструкция по установке на Open Server

## Настройки Open Server

Ваши текущие настройки:
- **HTTP**: Apache_2.4-PHP_8.0-
- **PHP**: PHP_8.1 ✅
- **MySQL**: MySQL-5.6 ✅

## Шаги установки

### 1. Размещение файлов

Скопируйте папку `clinicaphp.md` в директорию Open Server:
```
D:\OSPanel\domains\clinici.md\clinicaphp.md
```

Или создайте виртуальный хост в Open Server для домена `clinici.md`.

### 2. Создание базы данных

1. Откройте Open Server → **Дополнительно** → **MySQL 5.6** → **phpMyAdmin**
2. Создайте новую базу данных:
   - Имя: `dental_clinics`
   - Кодировка: `utf8mb4_unicode_ci`
3. Импортируйте структуру:
   - Выберите базу `dental_clinics`
   - Перейдите на вкладку **Импорт**
   - Выберите файл `database/structure.sql`
   - Нажмите **Вперед**

### 3. Настройка конфигурации

#### config/database.php
```php
'host' => 'localhost',
'dbname' => 'dental_clinics',
'username' => 'root',
'password' => '', // Пустой пароль для Open Server
```

#### config/config.php
```php
// Если используете домен clinici.md
define('BASE_URL', 'http://clinici.md/clinicaphp.md');

// Или если используете localhost
define('BASE_URL', 'http://localhost/clinicaphp.md');
```

### 4. Права доступа

Убедитесь, что папка `images/` имеет права на запись:
- Windows: Обычно права уже есть
- Если нужно: Свойства папки → Безопасность → Добавить права записи

### 5. Проверка .htaccess

Apache 2.4 в Open Server должен поддерживать mod_rewrite по умолчанию.
Если ЧПУ не работает:
1. Откройте `D:\OSPanel\modules\apache\conf\httpd.conf`
2. Убедитесь, что строка `LoadModule rewrite_module modules/mod_rewrite.so` не закомментирована
3. Найдите секцию `<Directory>` и убедитесь, что `AllowOverride All`

### 6. Запуск

1. Запустите Open Server (зеленая иконка)
2. Откройте в браузере:
   - `http://clinici.md/clinicaphp.md/` (если настроен домен)
   - или `http://localhost/clinicaphp.md/`

### 7. Доступ к админке

URL: `http://clinici.md/clinicaphp.md/administrator/`

Логин: `admin`
Пароль: `admin123` (по умолчанию, смените в `config/config.php`)

## Особенности MySQL 5.6

Проект адаптирован под MySQL 5.6:
- ✅ JSON поля хранятся как TEXT с автоматическим кодированием/декодированием
- ✅ Все функции работают так же, как с MySQL 8.0
- ✅ Полная совместимость с существующим кодом

## Проверка работы

После установки проверьте:
1. ✅ Главная страница открывается
2. ✅ Админка открывается и можно войти
3. ✅ Можно создать город
4. ✅ Можно создать клинику
5. ✅ Страница клиники отображается

## Решение проблем

### Ошибка подключения к БД
- Проверьте, что MySQL запущен в Open Server
- Проверьте настройки в `config/database.php`
- Проверьте логи: `D:\OSPanel\modules\apache\logs\`

### Ошибка 404 на всех страницах
- Проверьте, что mod_rewrite включен
- Проверьте `.htaccess` файл
- Попробуйте открыть `http://clinici.md/clinicaphp.md/index.php` напрямую

### Ошибки с JSON полями
- Убедитесь, что используется адаптированная версия Database.php
- Проверьте, что в БД поля типа TEXT, а не JSON

## Готово!

Проект должен работать на вашем Open Server! 🚀


